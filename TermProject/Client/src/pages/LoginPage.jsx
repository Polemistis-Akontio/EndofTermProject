function LoginPage() {
    

    return(
        <div>
            <button id="login">Login</button>
            <button id="newAccount">Create New Account</button>
        </div>
    )
}

export default LoginPage;